import React from 'react';

const SearchResult = ({ searchResult }) => {
    return (
        <div className="card">
            {searchResult.id}
        </div>
    );
};

export default SearchResult;